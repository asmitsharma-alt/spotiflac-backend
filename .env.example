# Server Port (Render dynamically assigns PORT)
PORT=8000

# Optional: Public URL for keep-alive self-ping (Render sets RENDER_EXTERNAL_URL automatically)
# SERVER_URL=https://spotiflac-api.onrender.com

# Optional custom lossless provider tokens (if you have personal accounts)
# QOBUZ_TOKEN=
# TIDAL_CUSTOM_API=
